<div class="row">
    <div class="col-md-12">
        <h1>{{restaurant.name}}</h1>
        <p>{{cat.name}}</p>
    </div>
    <div class="col-md-4">
        <img class="img-responsive center-block" src="{{restaurant.image}}" style="width: 250px;margin-bottom: 25px;">
    </div>
    <br/>
    <hr/>
    <br/>
    <div class="col-md-8">
        <ul class="list-group">
            <li class="list-group-item"><strong>評論數 : </strong><small>{{postLength restComment}}</small></li>
            <li class="list-group-item"><strong>瀏覽次數 : </strong><small>{{restaurant.viewCounts}}</small></li>
            <li class="list-group-item"><strong>收藏次數 : </strong><small>3</small></li>
        </ul>
    </div>
</div>